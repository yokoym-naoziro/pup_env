# puppeteer

## puppetコンテナ起動
```
docker-compose build 
docker-compose up -d
```
## コンテナに入る
```
docker exec -it puppet_demo /bin/bash
```
## puppetコンテナ内で実行
```
// node [ファイル名] で実行
# node example.js
```
